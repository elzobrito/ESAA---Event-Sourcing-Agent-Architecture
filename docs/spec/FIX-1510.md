# FIX-1510 — Especificacao

## MIN_CHECKS por task_kind

### Criterios

1. MIN_CHECKS = {spec: 1, impl: 1, qa: 1, hotfix: 2}.
2. Aplicar via dict-lookup com fallback p/ hotfix em validator.validate_agent_output.
3. Mantem reject_code MISSING_VERIFICATION (contrato).
