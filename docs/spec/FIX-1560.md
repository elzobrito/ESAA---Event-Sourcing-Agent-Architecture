# FIX-1560 — Especificacao

## Loader generico de plugins

### Criterios

1. glob '.roadmap/roadmap.*.json' (exceto roadmap.json).
2. Mescla tasks deduplicando por task_id (primeira ocorrencia vence).
3. Preserva project.name/audit_scope do primeiro plugin.
4. Mantem load_audit_seed como backcompat (delega para load_plugin_seeds).
