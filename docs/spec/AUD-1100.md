# AUD-1100 — Especificacao de auditoria

## Conformidade de schema e drift de versao

### Criterios verificaveis (invariantes)

1. roadmap.json/issues.json/lessons.json validos contra seus *.schema.json.
2. schema_version dos dados coerente com a versao dos contratos vigentes.
3. meta.run com projection_hash_sha256 (64 hex) e verify_status no enum.
4. indexes by_status/by_kind coerentes com as tasks.

> Fonte canonica: contratos em `.roadmap/` (v0.4.1) e codigo do runtime em `src/esaa/`.
